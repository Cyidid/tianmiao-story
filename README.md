# 甜喵物语

甜喵物语是一个卡通动漫风格的轻量 macOS 桌宠。它会漂浮在桌面上，支持拖拽、右键菜单、自由游走、跟随鼠标、角落休息、随机眨眼/跳跃/梳毛/睡觉、状态养成、心情气泡、专注计时，以及定时休息提醒。

当前正式版本为 **v3.19**。详细改动见 [`CHANGELOG.md`](CHANGELOG.md)。

## 设计方向

这版把主流桌宠常见做法落到一个很小的原生 macOS 应用里：

- 低打扰：默认是浮动小窗口，不抢 Dock，不遮挡正常工作流。
- 可互动：点击会有反馈，拖拽后会恢复自由游走。
- 有状态：甜喵会随机眨眼、跳跃、梳毛、睡觉，并用独立关节完成动作。
- 可养成：记录饱腹、开心、精力，支持喂小鱼干、陪它玩、摸摸头。
- 可表达：状态变化会用桌面气泡表达，不必频繁打断用户。
- 可控：右键菜单可以切换游走、跟随鼠标、角落休息，也能调大小和速度。
- 尺寸克制：默认按素材真实比例显示为小桌宠，避免挡住桌面内容。
- 贴地行走：自由游走只沿桌面底部水平移动，会自然停步、观察、换方向和继续行走，不再斜向漂浮。
- 动作质感：头、躯干、前爪、后身和尾巴分层驱动；走路时双爪反相摆动，跳跃包含下蹲与收爪，梳毛由单爪抬到脸侧，睡眠使用独立呼吸和尾部动作。
- 猫咪行为：加入前身下压、左右前爪交替、后身支撑和尾巴平衡的挠抓动作，可从右键菜单触发，也会在停步时偶尔出现。
- 互动反馈：点击时头部先看向触点，对应前爪和尾巴随后响应，身体通过缓入缓出的轻回弹恢复；拖拽结束使用独立落地动作，避免多套动画叠加。
- 素材处理：运行素材采用统一的 `360x392` 透明画布和重叠关节连接，不再通过整张动作图片轮回播放。

## 素材说明

当前应用使用 `Resources/` 下的两套共十三张透明骨骼部件 PNG 和 `AppIcon.icns`：正面坐姿用于互动，四足侧身姿态用于行走。多角度动漫源图保存在 `assets/source/multiview/`，`scripts/generate_rig_parts.py` 会生成两套动画部件，`scripts/generate_app_icon.py` 可根据当前 `normal.png` 重新生成应用图标。
- 素材生成会二次清除伪透明棋盘格和缩放产生的灰白边缘，窗口及猫咪轮廓外保持透明。
- 行走采用四腿对角交替步态，脚掌落地、承重和回摆阶段会同步桌面位移速度。
- 互动动作按身体支撑关系设计：真实闭眼、预蹲起跳、单爪梳毛与挠抓、带重心转移的打滚及闭眼睡眠。
- 对话气泡会按内容缩放并跟随猫咪可见轮廓，行走和互动使用头、脊柱、骨盆联动以减少纸片感。
- 行走底模已移除颈前误切入的腾跃前腿，改为肩下双前腿和更清晰的四足对角步态。
- 行走模型使用躯干父层统一带动身体、近侧前腿和头部，局部头部动作继承躯干变换，避免头颈割裂。
- 坐姿模型同样使用躯干父层带动胸口、头部和前爪，呼吸、梳毛与睡眠保持身体连接；走停切换加入缓入缓出淡化，减少姿态跳变。
- 轻提醒：保留桌宠作为温和提醒媒介的思路，每 45 分钟提醒休息喝水，可开启勿扰模式。
- 专注陪伴：可以让甜喵进入角落休息，并启动 25 分钟专注计时。

## 构建

需要 macOS 和 Xcode Command Line Tools：

```bash
./scripts/build_app.sh
```

构建结果会生成本地 `甜喵物语.app`。该目录是构建产物，不提交到仓库；发布版请从 GitHub Releases 下载 zip。
应用版本与构建号统一维护在 `Config/version.env`，正式发布前只需更新这一处。

正式发布包使用：

```bash
./scripts/package_release.sh
```

脚本会重新构建应用，在 `dist/` 生成带版本号的 ZIP 和 SHA-256 文件，并重新解压检查签名及版本号。

## 文件结构

- `Sources/TianMiao/main.swift`: 当前可维护源码。
- `Config/version.env`: 应用版本号与构建号。
- `Resources/`: 应用运行时使用的图片帧和 `AppIcon.icns`。
- `assets/source/multiview/`: 多角度动漫源图。
- `scripts/`: 构建、发布打包、素材生成和图标生成脚本。
